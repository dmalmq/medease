<div class="w-full p-4 sticky top-0 border border-gray-300 rounded-md bg-slate-50" data-controller="booking-form" data-booking-form-room-price-value="<%= @room.price_per_hour %>">
  <%= simple_form_for([@room, @booking]) do |f| %>

    <%= f.input :date, html5: true %>
    <%= f.input :startFrom, label: "From:", html5: true %>
    <%= f.input :hours, input_html: {
      class: 'hour-input',
      id: 'hour-input',
      min: 1,
      data: {
        action: "change->booking-form#calculation",
      }
    } %>
    <%= f.input :comment, label: "Operation details:" %>
    <p>Total price: ¥ <span id="total-price" data-booking-form-target="totalPrice"><%= @room.price_per_hour %></span></p>
    <%= f.button :submit, class: "btn"%>
  <% end %>
</div>
