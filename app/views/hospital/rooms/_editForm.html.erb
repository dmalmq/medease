<%= simple_form_for([:hospital, @room]) do |f| %>
  <%= f.input :name %>
  <%= f.input :category, collection: Room::CATEGORIES, selected: "General Surgery" %>
  <%= f.input :equipment, collection: Room::EQUIPMENTS, selected: "" %>
  <%= f.input :price_per_hour %>
  <%= f.input :capacity, collection: (1..20).to_a %>
  <%= f.input :photos, as: :file, input_html: { multiple: true } %>
  <%= f.submit class: 'btn btn-primary' %>
<% end %>
