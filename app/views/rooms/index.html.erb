<div class="entire" data-controller="booking-form" data-action= "change->booking-form#filtering" data-booking-form-target="searchBox">
  <%= render "shared/banner" %>
  <div class="flex justify-content-center mx-auto">
    <div class="col-sm-8 mt-10">
      <%= form_with url: rooms_path, method: :get, class: "flex gap-10" do |form| %>
        <%= render "shared/date" %>
        <%= text_field_tag :query,
            params[:query],
            class: "",
            placeholder: "Type your address"
        %>
        <div class="row w-full flex-1 mr-1"><%= select_tag :category, options_for_select(Room::CATEGORIES, params[:category]), prompt: "Select a category" %> </div>
        <div class="row w-full flex-1"><%= select_tag :equipment, options_for_select(Room::EQUIPMENTS, params[:equipment]), prompt: "Select an equipment" %></div>
        <%= submit_tag "Search", name: "", class: "bg-blue-500 hover:bg-blue-700 text-white py-2 px-10 rounded" %>
      <% end %>
    </div>
  </div>
  <div class="row justify-content-center">
    <div class="col-sm-8 my-3">
    </div>
  </div>
  <div class="mx-4 mt-6">
    <div class="grid grid-cols-3 gap-6">

      <% @rooms.each do |room| %>
        <div
  class="block rounded-lg bg-white shadow-[0_2px_15px_-3px_rgba(0,0,0,0.07),0_10px_20px_-2px_rgba(0,0,0,0.04)]">
          <%= link_to room_path(room) do %>
            <%= cl_image_tag room.photos.first.key, alt: "room image", class: 'roomPic rounded-t-lg bg-cover w-full h-80 bg-center' %>
          <% end %>
          <div class="px-4 py-4">
          <div class="flex justify-between">
            <h2 class="text-xl pb-0 pt-0 font-bold" > <%= room.facility_name %> </h2>
            <p class="text-sm font-bold italic"><%= room.address %></p>
          </div>
          <div class="pb-0 pt-0 mb-1 text-md font-medium text-neutral-800 flex justify-between">
            <p class="font-medium text-md"><%= room.category %></p>
            <p class="text-sm font-bold"></p>
          </div>
          <div class="pb-0 pt-0 flex justify-between">
            <p>¥<%= room.price_per_hour %> /hr</p>
            <p class="text-slate-400">Capacity: <%= room.capacity %> People</p>
          </div>
            </p>
          </button>
        </div>
      </div>
    <% end %>
  </div>
</div>















      <!--
      <% @rooms.each do |room| %>
        <div
  class="block rounded-lg bg-white shadow-[0_2px_15px_-3px_rgba(0,0,0,0.07),0_10px_20px_-2px_rgba(0,0,0,0.04)]">
          <%= link_to room_path(room) do %>
            <%= cl_image_tag room.photos.first.key, alt: "room image", class: 'roomPic rounded-t-lg bg-cover w-full h-80 bg-center' %>
        <% end %>
        <div class="px-4 py-4">
          <div class="flex justify-between">
            <h2 class="text-xl pb-0 pt-0 font-bold" > <%= room.facility_name %> </h2>
            <p class="text-sm font-bold italic"><%= room.address %></p>
          </div>
          <div class="pb-0 pt-0 mb-1 text-md font-medium text-neutral-800 flex justify-between">
            <p class="font-medium text-md"><%= room.category %></p>
            <p class="text-sm font-bold"></p>
          </div>
          <div class="pb-0 pt-0 flex justify-between">
            <p>¥<%= room.price_per_hour %> /hr</p>
            <p class="text-slate-400">Capacity: <%= room.capacity %> People</p>
          </div>
        </p>
      </button>
    </div>
  </div>
</div>
<% end %>
</div>
</div>
